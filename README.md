# QQ Pet Interface Copilot / QQ 宠物接口助手

中文：一个正在验证中的电脑端 QQ 宠物自动控制工具。项目复用已登录电脑版 QQ/NapCat 的会话、签名和加密封包，通过本机 OneBot `send_packet` 调用宠物 OIDB 接口，不需要长期连接手机或 ADB。

English: A PC-side QQ pet automation assistant under active validation. It reuses the session, signing, and encrypted packet capability of an already logged-in QQ/NapCat instance, then calls pet OIDB APIs through local OneBot `send_packet`; it does not require a persistent phone or ADB connection.

> 中文：当前仓库用于展示阶段性成果，处于公开预览阶段。请只操作自己的 QQ 账号，并自行承担非公开接口发生变化的风险。
>
> English: This repository is a public preview of work in progress. Use it only with your own QQ account and accept the risk that undocumented interfaces may change.

## 已验证成果

| 能力 | 当前状态 | 实测依据 |
| --- | --- | --- |
| 状态读取 | 可用 | 金币、心情、体力、清洁、综合值 |
| 食物与洗护库存 | 可用 | 饼干、虾仁、香皂片、沐浴球 |
| 自动补货与喂食 | 可用 | 金币购买后库存增加，喂食后体力增加 |
| 自动补货与洗澡 | 可用 | 购买香皂片后清洁和心情增加 |
| 学习 | 已跑通 | 当前学园阶段与课程目录、真实开课、`storyId` 和 1800 秒倒计时均由电脑端验证 |
| 打工 | 已跑通 | 真实任务倒计时、体力/清洁消耗和金币奖励 |
| 冒险 | 已跑通 | 45 秒任务、状态变化和奖励结算 |
| 自动 PK | 已跑通 | 电脑端直接读取战力、开始 8 秒 PK、结算并核对金币/心情/体力/清洁变化 |
| 每日计数与幂等 | 可用 | 跨天归档、重启恢复、按 `storyId` 防重复结算 |
| 图形界面 | 可用 | 实时状态（含食物与洗护库存）、日志、开始/停止、服务器课程、岗位与冒险下拉框和设置页 |
| 好友每日访问 | 开发中 | 好友列表、白名单/排除名单、每日进度文件和前端入口已完成；真实访问与踩踩等待 Hook 协议样本 |

本地协议、客户端解析和调度测试目前为 **35/35 通过**。

## 调度逻辑

- 体力或清洁低于默认阈值 80 时，优先照顾宠物；库存不足可自动用金币补货。
- 自动 PK 到达配置时间后执行；默认从服务器好友宠物池中选择对手，单个好友连续成功 3 次后自动切换下一位。会过滤白名单、排除名单、失败冷却和战力不符合条件的好友。每局消耗体力 5、清洁 5，约 8 秒后结算。成功必须看到服务器状态变化，不能只看成功码。
- PK 每日成功、失败、金币收益和 storyId 写入 `runs/pk-日期.json`，重启后继续当天计数，第二天自动使用新文件。
- 配置时间后优先尝试冒险。
- 金币充足时优先学习，否则尝试打工。
- 学习会读取服务器当前学园阶段和实时课程目录，按力量/智力/魅力选择当前可执行的最高收益课程，再调用真实开课接口。
- 设置页可刷新当前阶段课程，选择“自动最高收益”或固定某一门课程，保存后下一轮立即生效。
- 打工会读取服务器开放职业和实时岗位目录，自动选择总收益最高岗位，也可以在前端固定职业与岗位；开工使用职业模块的真实 `6400` 请求。
- 冒险会读取服务器实时选项（当前正式选项为“打招呼”），只选择服务器标记为可执行的项目，再调用真实 `6700` 开始接口。
- 设置页可以刷新并固定服务器返回的冒险选项，不再使用写死的“金币/技能/气候”假路线。
- 任务完成后按 `storyId` 结算并持久化，避免对旧任务反复结算。
- NapCat 接口连接超时后自动进入重连状态，默认从 3 秒开始指数退避、最长等待 60 秒；恢复后继续下一轮，不会重发刚才可能已经送达的写指令。
- 主任务连续失败达到阈值后，可发送 Windows Toast，并通过 OnePush 分发到 Bark、PushPlus、Server酱、SMTP 和自定义 webhook；包含重复告警冷却与恢复通知。

## 当前限制

- 好友雇佣的开工字段已经恢复，但服务器尚未返回可用雇佣列表，因此自动选择好友仍未完成；无好友时会按正式协议正常开工。
- 好友列表与每日断点续跑框架已完成；访问好友和“踩踩”写接口必须取得真实动态路径后才会开放。
- 自动 PK 已实现服务器好友宠物池分页读取和轮换；当前电脑 QQ 会话若对好友池命令返回空包，会明确记录并临时使用已验证的备用对手，不会把空成功码误报成“已读取全部好友”。
- 被雇佣召回支持“等到 25/75（收益分成最高）”和“立刻召回”两种策略；召回使用服务器结算回包验证、按 storyId 去重并写入每日计数。
- 冒险奖励由服务器在结算时决定，目录可能只给出事件描述而不提前显示奖励。
- 项目依赖 NapCat 提供的原始封包能力，尚不是独立 QQ 登录客户端。

## 快速开始

### 一键启动（Windows）

双击 `start-interface-copilot.bat`，启动器会依次完成：

1. 检查电脑版 QQ、NapCatQQ Desktop 和本机 OneBot 接口。
2. NapCat 未安装时，从官方 GitHub 下载 Windows x64 MSI，核对官方 SHA-256 后启动安装。
3. 启动 NapCat；QQ 尚未登录时等待用户扫码或完成 QQ 登录确认。
4. 从 NapCat `get_login_info` 读取当前 QQ 账号，从本机 NapCat 配置读取接口端口和令牌；若尚未配置 HTTP 服务，会仅增加一个随机令牌、仅监听 `127.0.0.1` 的本机接口。
5. 验证宠物状态后保存本机配置并打开控制台。

首次接入尚未记录的宠物时，需要输入一次 `petId`；验证通过后不会再次询问。启动器不会导出 QQ 会话，也不会把接口令牌上传到网络。

也可以下载 GitHub Actions 生成的 `QQ-Pet-Interface-Copilot-Windows-x64.zip`，解压后直接运行 `QQ宠物助手.exe`。

### 开发者启动

准备已登录目标账号的电脑版 QQ 和 NapCat，使用 Python 3.11+ 运行：

```powershell
py -3 launcher.py
```

首次接入新账号时建议保留安全模式，确认状态读取正常后再开放写操作。

## 运行流程

```text
电脑版 QQ 登录
      ↓
NapCat 使用本机会话并开放 127.0.0.1 OneBot 接口
      ↓
一键启动器发现账号、接口端口与本机令牌
      ↓
QQ 宠物助手读取服务器状态和动态规则
      ↓
统一调度器执行照顾、学习、打工、冒险和 PK
      ↓
重新读取服务器状态验证结果，并写入 runs/ 每日进度
```

本项目不包含 QQ 或 NapCat。GPL-3.0-only 只覆盖本仓库代码；QQ 与 NapCat 分别遵循其自身许可和使用条款。正常运行无需 ADB，ADB 仅用于协议研究阶段的调试。

## 测试

```powershell
py -3 -m unittest discover -s tests -v
```

## 安全说明

- `config.yaml`、运行日志、抓包、APK、设备提取物和账号响应均被 `.gitignore` 排除。
- 不要把 OneBot 端口监听到公网，也不要提交真实 UIN、`pet_id` 或访问令牌。
- 仓库不包含 QQ、NapCat、APK 或其他第三方二进制文件。

## 许可证

本项目采用 [GNU General Public License v3.0](LICENSE)（GPL-3.0-only）授权。任何再发布或修改后的版本均须遵守该许可证并提供相应源代码。
