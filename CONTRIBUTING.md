# 贡献指南

感谢你参与 QQ 宠物接口助手。提交贡献前，请先阅读 README 中的当前能力、限制和安全说明。

## 开始之前

1. 搜索现有 Issue，确认问题尚未被报告或实现。
2. 较大的功能或协议调整请先创建 Issue，说明目标、范围和验证方式。
3. 测试时仅使用自己的 QQ 账号，不要提交真实 UIN、`pet_id`、访问令牌、Cookie、抓包、APK 或运行日志中的隐私数据。

## 本地开发

建议使用 Python 3.11 或更高版本。复制 `config.example.yaml` 为 `config.yaml`，只在本地填写配置；`config.yaml` 不应提交。

运行应用：

```powershell
py -3 main.py
```

运行测试：

```powershell
py -3 -m unittest discover -s tests -v
```

## 提交 Pull Request

- 从最新主分支创建独立分支，一个 PR 只处理一个主题。
- 保持改动尽量小，并补充或更新相关测试与文档。
- PR 描述中写清动机、主要改动、测试结果和潜在风险。
- 涉及界面调整时附截图；涉及接口时说明验证条件，但不要附真实账号数据或未脱敏封包。
- 提交前确认测试通过，并检查 `git diff` 中没有密钥和个人信息。

维护者可能要求调整实现、补充测试或缩小范围。提交贡献即表示你同意按仓库的 GPL-3.0 许可证发布该贡献。

